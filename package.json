{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "generate:prod": "NODE_ENV=production nuxt generate",
    "deploy": "npm run generate:prod && touch .output/public/.nojekyll && npx gh-pages -d .output/public"
  },
  "dependencies": {
    "nuxt": "^3.17.4",
    "vue": "^3.5.15",
    "vue-router": "^4.5.1",
    "vue3-carousel": "^0.15.1"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "sass": "^1.89.0",
    "sass-loader": "^16.0.5"
  }
}
